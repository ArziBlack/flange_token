pub struct MetadataInfo {
    pub name: String,   // The name of the token (e.g., "MyToken").
    pub symbol: String, // The symbol of the token (e.g., "MTK").
    pub uri: String,    // The URI pointing to the metadata JSON.
}
